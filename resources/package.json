{
  "name": "lambda-cognito-link-federated-user",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "@types/node": "^25.0.2",
    "@aws-sdk/client-cognito-identity-provider": "^3.952.0",
    "esbuild": "^0.27.1"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --platform=node --target=node22 --outdir=dist"
  }
}
